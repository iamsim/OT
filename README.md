*Run <code>npm install -g ionic cordova</code> to install latest ionic and cordova*
1. Clone to desktop or wherever
2. CD into the project and run <code>bower install</code>
3. Run <code>ionic serve</code>

